<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<button
		class="navbar-toggler"
		type="button"
		data-toggle="collapse"
		data-target="#navbarNavAltMarkup"
		aria-controls="navbarNavAltMarkup"
		aria-expanded="false"
		aria-label="Toggle navigation"
	>
		<span class="navbar-toggler-icon"></span>
	</button>
	<div class="collapse navbar-collapse" id="navbarNavAltMarkup">
		<ul class="navbar-nav mr-auto">
		<li class="nav-item">
			<a class="nav-link <%= (toggle == 'shop' ? 'active' : '') %>" href="/"
			>Shop</a
			>
		</li>
		<li class="nav-item">
			<a
			class="nav-link <%= (toggle == 'products' ? 'active' : '') %>"
			href="/products"
			>Products</a
			>
		</li>
		<% if (isAdmin) { %>
		<li class="nav-item">
			<a
			class="nav-link <%= (toggle == 'orders' ? 'active' : '') %>"
			href="/orders"
			>Orders</a
			>
		</li>
		<li class="nav-item">
			<a
			class="nav-link <%= (toggle == 'cart' ? 'active' : '') %>"
			href="/cart"
			>Cart</a
			>
		</li>
			<li class="nav-item dropdown">
			<a
				class="nav-link dropdown-toggle <%= (toggle == 'admin' ? 'active' : '') %>"
				href="#"
				id="navbarDropdown"
				role="button"
				data-toggle="dropdown"
				aria-haspopup="true"
				aria-expanded="false"
			>
				Admin
			</a>
			<div class="dropdown-menu" aria-labelledby="navbarDropdown">
				<a class="dropdown-item" href="/admin/list-products"
				>Admin Products</a
				>
				<div class="dropdown-divider"></div>
				<a class="dropdown-item" href="/admin/add-product">Add Product</a>
			</div>
			</li>
		<% } %>
		<!-- <li class="nav-item">
			<a
			class="nav-link <%= (toggle == 'about' ? 'active' : '') %>"
			href="/about"
			>About</a
			>
		</li> -->
	</ul>
	<ul class="navbar-nav ml-auto">
		<% if(!isAdmin)  {%>
			<li class="nav-item">
				<a
				class="nav-link <%= (toggle == 'login' ? 'active' : '') %>"
				href="/auth/login"
				>Login</a
				>
			</li>
			<li class="nav-item">
				<a
				class="nav-link <%= (toggle == 'signup' ? 'active' : '') %>"
				href="/auth/signup"
				>Sign Up</a
				>
			</li>
		<%} else {%>
			<li class="nav-item">
				<form action="/auth/logout" method="post">
					<input type="hidden" name="_csrf" value="<%= csrf %>">
					<button class="btn nav-link" type="submit">Logout</button>
				</form>
			</li>
		<%} %>
	</ul>
	</div>
</nav>
